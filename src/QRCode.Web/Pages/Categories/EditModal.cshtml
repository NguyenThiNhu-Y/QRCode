@page
@using QRCode.Localization
@using Microsoft.Extensions.Localization
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal

@model QRCode.Web.Pages.Categories.EditModalModel
@inject IStringLocalizer<QRCodeResource> L

@section scripts{
    <script type="text/javascript" src="/Pages/Categories/Index.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#summernote').summernote();

        });

    </script>
}
<form method="post" asp-page="/Categories/EditModal">
    <abp-card size="ExtraLarge">
        <abp-card-header>
            <abp-row>
                <abp-column size-md="_6">
                    <abp-card-title style="font-weight:bold; text-transform:uppercase; font-size:20px">@L["EditCategory"]</abp-card-title>
                </abp-column>

            </abp-row>
        </abp-card-header>
        <abp-card-body>
            <abp-input asp-for="Id" />
            <abp-input asp-for="Category.Name" required />
            <abp-select asp-for="Category.IdParent" asp-items="Model.CategoryIdFilterItems" />

            <abp-input asp-for="Category.Deadline" min="1" />
            <textarea asp-for="Category.Description" id="summernote"></textarea>
        </abp-card-body>
        <abp-card-footer>
            <a href="/Categories" class="btn btn-secondary">@L["Cancel"]</a>
            <button id="save" data-busy-text="Đang lưu..." class="btn btn-primary bkgrnd-cyan save-details" type="submit" name="save-details">@L["Save"]</button>
        </abp-card-footer>
    </abp-card>
</form>
